version: "2"

run:
  timeout: 3m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  settings:
    govet:
      enable-all: true
      disable:
        - fieldalignment  # noisy for MVP
